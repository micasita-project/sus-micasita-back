# PostgreSQL connection string
# Supabase: copiar desde Project Settings > Database > Connection string
DATABASE_URL=postgresql://usuario:contraseña@host:5432/nombre_db

# Para Supabase o cualquier Postgres que requiera SSL
PGSSLMODE=require

# URL(s) del frontend separadas por coma (sin barra final)
FRONTEND_URL=https://tu-app.netlify.app

# Puerto (Railway lo inyecta automáticamente, no cambiar)
PORT=3000
